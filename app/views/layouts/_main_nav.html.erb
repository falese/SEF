<nav class="navbar navbar-custom navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">
                <i class="fa fa-bars"></i>
            </button>
            </a>
            <% if current_user %>
              <%= image_tag(current_user.image_url, class:'img-thumbnail img-rounded') %>
              <br />
              <%= link_to "Sign out", signout_path, id: "sign_out",class:'btn btn-default btn-xs'%>
            <% else %>
              <%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in",class:'btn btn-default btn-xs' %>
            <% end %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
            <% if current_user %>
            <ul class="nav navbar-nav">
                <!-- Hidden li included to remove active class from about link when scrolled up past about section -->

                <li class="hidden">
                  <% if current_user %>
                    <%= image_tag(current_user.image_url, class:'img-thumbnail img-rounded') %>
                    <br />
                    <%= link_to "Sign out", signout_path, id: "sign_out",class:'btn btn-default btn-xs'%>
                  <% else %>
                    <%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in",class:'btn btn-default btn-xs' %>
                  <% end %>
                </li>
                <li>
                    <%= link_to "Home", user_path(current_user.id),class:'btn btn-default btn-xs' %>
                </li>
                <li>
                  <% if current_user.accounts.empty? %>

                  <%= link_to 'Add External Accounts', user_api_accounts_path(current_user), class:'btn btn-default btn-large' %>
                  <% else %>
                  <%= link_to 'Refresh Accounts', user_api_accounts_path(current_user), class:'btn btn-default btn-large' %>

                  <% end %>
                </li>

                <li>

                </li>
            </ul>
<% end %>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>
